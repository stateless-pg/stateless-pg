---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: tenants.core.stateless-pg.io
spec:
  group: core.stateless-pg.io
  names:
    categories:
    - stateless-pg
    kind: Tenant
    listKind: TenantList
    plural: tenants
    shortNames:
    - tn
    singular: tenant
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.databaseName
      name: Database
      type: string
    - jsonPath: .status.conditions[?(@.type == 'Available')].status
      name: Available
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Tenant is the Schema for the tenants API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Tenant
            properties:
              basebackupCacheEnabled:
                default: false
                description: basebackupCacheEnabled enables basebackup caching
                type: boolean
              checkpointDistance:
                default: 268435456
                description: checkpointDistance defines the size threshold between
                  checkpoints (L0 layer file size)
                format: int64
                minimum: 0
                type: integer
              checkpointTimeout:
                default: 10m
                description: checkpointTimeout defines the maximum time between checkpoints
                type: string
              compactionAlgorithm:
                default: Legacy
                description: compactionAlgorithm defines the algorithm to use for
                  layer compaction
                type: string
              compactionL0First:
                default: true
                description: compactionL0First enables L0 compaction pass for responsiveness
                type: boolean
              compactionL0Semaphore:
                default: true
                description: compactionL0Semaphore enables L0 compaction semaphore
                  for concurrency control
                type: boolean
              compactionPeriod:
                default: 20s
                description: compactionPeriod defines how often to run compaction
                type: string
              compactionShardAncestor:
                default: true
                description: compactionShardAncestor enables shard-aware compaction
                type: boolean
              compactionTargetSize:
                default: 134217728
                description: compactionTargetSize defines the target layer size for
                  image/delta layers (L1 layer file size)
                format: int64
                minimum: 0
                type: integer
              compactionThreshold:
                default: 10
                description: compactionThreshold defines the threshold for triggering
                  compaction
                format: int32
                minimum: 0
                type: integer
              compactionUpperLimit:
                default: 10
                description: compactionUpperLimit defines the maximum layers to compact
                  before memory limits
                format: int32
                minimum: 0
                type: integer
              evictionPolicy:
                default: NoEviction
                description: evictionPolicy defines the page eviction strategy
                enum:
                - NoEviction
                - AlwaysEvict
                type: string
              evictionsLowResidenceDurationMetricThreshold:
                default: 24h
                description: evictionsLowResidenceDurationMetricThreshold defines
                  the metric threshold for low residence time
                type: string
              gcCompactionEnabled:
                default: true
                description: gcCompactionEnabled enables GC compaction pass
                type: boolean
              gcCompactionInitialThresholdKb:
                default: 5368709
                description: gcCompactionInitialThresholdKb defines the initial threshold
                  for GC compaction in KB
                format: int64
                minimum: 0
                type: integer
              gcCompactionRatioPercent:
                default: 100
                description: gcCompactionRatioPercent defines the compaction ratio
                  percentage for GC
                format: int64
                maximum: 100
                minimum: 0
                type: integer
              gcCompactionVerification:
                default: true
                description: gcCompactionVerification verifies GC compaction results
                type: boolean
              gcHorizon:
                default: 67108864
                description: gcHorizon defines how far back to retain data
                format: int64
                minimum: 0
                type: integer
              gcPeriod:
                default: 1h
                description: gcPeriod defines how often to run garbage collection
                type: string
              heatmapPeriod:
                default: 0s
                description: heatmapPeriod defines the heatmap refresh period (0 =
                  disabled)
                type: string
              imageCreationPreemptThreshold:
                default: 3
                description: imageCreationPreemptThreshold preempts image creation
                  if L0 backpressure
                format: int32
                minimum: 0
                type: integer
              imageCreationThreshold:
                default: 3
                description: imageCreationThreshold defines after how many L0 layers
                  to create an image layer
                format: int32
                minimum: 0
                type: integer
              imageLayerCreationCheckThreshold:
                default: 2
                description: imageLayerCreationCheckThreshold defines how many L0
                  layers to ingest WAL for before checking image creation
                format: int32
                minimum: 0
                type: integer
              imageLayerForceCreationPeriod:
                description: imageLayerForceCreationPeriod forces image layer creation
                  at interval
                type: string
              l0FlushDelayThreshold:
                description: l0FlushDelayThreshold defines the L0 flush delay threshold
                format: int32
                minimum: 0
                type: integer
              l0FlushStallThreshold:
                description: l0FlushStallThreshold defines the L0 stall threshold
                format: int32
                minimum: 0
                type: integer
              laggingWalTimeout:
                default: 10s
                description: laggingWalTimeout defines the timeout before disconnecting
                  slow WAL receiver
                type: string
              lazySlruDownload:
                default: false
                description: lazySlruDownload enables lazy download strategy
                type: boolean
              lsnLeaseLength:
                default: 10m
                description: lsnLeaseLength defines the LSN lease duration for read
                  consistency
                type: string
              lsnLeaseLengthForTs:
                default: 60s
                description: lsnLeaseLengthForTs defines the LSN lease duration for
                  timestamp queries
                type: string
              maxLsnWalLag:
                default: 1073741824
                description: maxLsnWalLag defines the maximum WAL lag allowed (supports
                  1GB/s throughput)
                format: int64
                minimum: 0
                type: integer
              minResidentSizeOverride:
                description: minResidentSizeOverride overrides the minimum resident
                  size threshold
                format: int64
                minimum: 0
                type: integer
              pitrInterval:
                default: 168h
                description: pitrInterval defines the point-in-time recovery window
                type: string
              relSizeV2Enabled:
                default: false
                description: relSizeV2Enabled enables relation size v2 calculation
                type: boolean
              relsizeSnapshotCacheCapacity:
                default: 1000
                description: relsizeSnapshotCacheCapacity defines the snapshot cache
                  size for relation size
                format: int32
                minimum: 0
                type: integer
              samplingRatio:
                description: samplingRatio defines the sampling configuration for
                  metrics
                properties:
                  ratio:
                    description: ratio defines the sampling ratio value
                    type: string
                type: object
              timelineGetThrottle:
                description: timelineGetThrottle defines the rate limiting configuration
                  for page reads
                properties:
                  enabled:
                    description: enabled defines if throttling is enabled
                    type: boolean
                  rps:
                    description: rps defines requests per second limit
                    format: int32
                    minimum: 0
                    type: integer
                type: object
              timelineOffloading:
                default: true
                description: timelineOffloading enables timeline offloading to secondary
                  nodes
                type: boolean
              walreceiverConnectTimeout:
                default: 10s
                description: walreceiverConnectTimeout defines the timeout for WAL
                  receiver connection
                type: string
            type: object
          status:
            description: status defines the observed state of Tenant
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the Tenant resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              phase:
                description: phase represents the phase of the tenant creation/deletion
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
